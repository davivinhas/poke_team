# Projeto FastAPI poke team

## Instalar dependencias

```bash
pip install -r requirements.txt
```

## Rodar em desenvolvimento

```bash
uvicorn app.main:app --reload
```

A API iniciara em `http://127.0.0.1:8000`.
